# Day 01 - Verify Docker Setup

## What This File Does
Verifies Docker is installed correctly and confirms the Docker engine is running with a successful test container.


## Task
Run Docker and inspect local engine state.

```bash
docker --version
docker info
docker run hello-world
docker images
docker ps -a
```

