# Day 10 - Docker Compose Basic Stack

## What This File Does
Uses Docker Compose commands to bring up, inspect, restart, and tear down a multi-service stack.


## Task
Run and manage a multi-container compose setup.

```bash
docker compose up -d
docker compose ps
docker compose logs --tail=100
docker compose restart
docker compose down
```

